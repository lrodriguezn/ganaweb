{
  "name": "@ganaweb/ui",
  "version": "0.0.0",
  "private": true,
  "description": "GanaWeb component library. Migrated ganado/ components + shadcn primitives. NOT dominio (dependency-cruiser enforced).",
  "type": "module",
  "sideEffects": ["**/*.css"],
  "scripts": {
    "build": "echo \"@ganaweb/ui: tsup build lands in PR4\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@ganaweb/ui: no tests in PR1 stub\"",
    "lint": "biome check .",
    "ci": "pnpm run typecheck && pnpm run lint && pnpm run test"
  },
  "dependencies": {},
  "peerDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@ganaweb/config": "workspace:*",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "typescript": "^5.7.2"
  }
}
