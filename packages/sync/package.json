{
  "name": "@ganaweb/sync",
  "version": "0.0.0",
  "private": true,
  "description": "GanaWeb sync protocol. Port interfaces only (push/pull/conflict-resolver). No implementation this change (D6).",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@ganaweb/sync: no tests in scaffold-monorepo (interfaces only)\"",
    "lint": "biome check ."
  },
  "dependencies": {},
  "devDependencies": {
    "@ganaweb/config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
